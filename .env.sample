# Environment Configuration for WP Multi-Updater
#
# Copy this file to .env and configure as needed.
# The .env file is gitignored for security.

# Slack webhook for notifications (future enhancement)
# SLACK_WEBHOOK=https://hooks.slack.com/services/YOUR/SECRET/TOKEN

# Custom SSH key path (if not using default ~/.ssh/id_rsa)
# SSH_KEY_PATH=/path/to/custom/key

# Database path override (default: state/results.sqlite)
# DB_PATH=/custom/path/to/database.sqlite

# Global timeout override (default: 900 seconds)
# TIMEOUT_SEC=1200

# Debug mode (set to "true" for verbose logging)
# DEBUG=false
